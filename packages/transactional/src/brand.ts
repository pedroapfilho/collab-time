/** Brand strings for the email package. Mirrors apps/web/src/lib/constants.ts, which it cannot import. */
const APP_NAME = "Collabtime";

const APP_URL = "https://collabtime.io";

const SECURITY_EMAIL = "security@collabtime.io";

const DEFAULT_FROM = `${APP_NAME} <noreply@email.collabtime.io>`;

export { APP_NAME, APP_URL, DEFAULT_FROM, SECURITY_EMAIL };
